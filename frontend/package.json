{
  "name": "erp-frontend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "React islands for the Sales & Inventory ERP. Builds into ../static/react/ — Django serves the output.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "watch": "vite build --watch"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^6.0.7"
  }
}
